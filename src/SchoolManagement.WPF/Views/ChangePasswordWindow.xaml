<Window x:Class="SchoolManagement.WPF.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تغییر رمز عبور" Height="360" Width="360"
        FlowDirection="RightToLeft" FontFamily="Segoe UI" FontSize="14"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" Text="لطفاً رمز عبور خود را تغییر دهید" FontSize="16" FontWeight="Bold" TextWrapping="Wrap"/>

        <TextBlock Grid.Row="2" x:Name="CurrentPasswordLabel" Text="رمز عبور فعلی"/>
        <PasswordBox Grid.Row="3" x:Name="CurrentPasswordBox" Height="32" Padding="6"/>

        <TextBlock Grid.Row="5" Text="رمز عبور جدید (حداقل ۶ کاراکتر)"/>
        <PasswordBox Grid.Row="6" x:Name="NewPasswordBox" Height="32" Padding="6"/>

        <TextBlock Grid.Row="8" Text="تکرار رمز عبور جدید"/>
        <PasswordBox Grid.Row="9" x:Name="ConfirmPasswordBox" Height="32" Padding="6"/>

        <TextBlock Grid.Row="11" x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed"/>

        <Button Grid.Row="13" x:Name="SubmitButton" Content="تغییر رمز عبور" Height="36" Click="SubmitButton_Click"/>
    </Grid>
</Window>
